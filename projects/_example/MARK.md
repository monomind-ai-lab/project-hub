# Mark — notes-api

The repository's basic information and URLs: enough to address it without a
checkout. Written by `project_hub.py init` from what it read, then yours to
correct. Plain Markdown on purpose — this is Hub metadata, not a record in the
`project-context/1` model, so it carries no frontmatter and no `kind`.

- Project id: `notes-api`
- Remote: `https://example.invalid/acme/notes-api.git`
- Host: `example.invalid`
- Default branch: `main`
- Visibility: `private`
- Languages: Python, SQL
- Entry points: `src/notes_api/__main__.py`, `Makefile`
- Issue tracker: `https://example.invalid/acme/notes-api/issues`
- CI: `https://example.invalid/acme/notes-api/actions`
- Deployment: `https://notes.example.invalid`
- Builders: `person:ren`, `person:mira`
- Project Context installed: yes
- Marked: 2026-09-03

## Notes

Anything the fields above cannot hold. Why this repository exists, what is odd
about it, who to ask. Two sentences beat a blank line.
