<!-- project-hub:unfilled -->
# Global summary

The L0 route. Every session in every project loads this first, so it is held to
150 words and says what to *do*, not what we *are*.

Answer three things and delete the line at the top of this file:

- What this organisation builds, in one sentence.
- The two or three constraints that decide most arguments.
- Where to look next: `IDENTITY.md` for the voice, `GUARDRAILS.md` for the
  rules, `GOALS.md` for what is being aimed at.
