# `global/shared/`

Records that are true across projects and fit none of the named files. Prefer a named file when one fits; this folder is the honest remainder, not a drawer.

Records here are pushed into every repository, so they are held to the same
400-word per-file budget as the rest of the global tier. Use
`templates/global/` for the shape.
