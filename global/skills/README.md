# `global/skills/`

One record per shared skill — versioned, with `learned_from` links to the records that taught it. An agent may propose a change to a skill; it may never approve, activate, or retire its own.

Records here are pushed into every repository, so they are held to the same
400-word per-file budget as the rest of the global tier. Use
`templates/global/` for the shape.
