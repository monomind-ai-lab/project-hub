# Epic — {{project_id}}

Authored here in the Hub, pushed down read-only. The repository's `PLAN.md`
names items from this file with a `Serves:` line, and the repository's doctor
errors on a plan item that names none. Budget: 600 words.

## Why this project exists

One paragraph. The problem, not the solution.

## What must be true when it is done

Each item gets a stable `E-NNN` id, because `PLAN.md` will name it and a
renumbered item silently breaks a `Serves:` line.

- **E-001 — …**
- **E-002 — …**

## What this project is not

The scope that was considered and ruled out. This paragraph prevents more work
than the one above.

## Known tensions

Where two items pull against each other. Say so here rather than letting a
builder discover it and pick one alone.
