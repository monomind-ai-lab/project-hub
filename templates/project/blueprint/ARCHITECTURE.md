# Architecture — {{project_id}}

The shape the code cannot express. Authored here in the Hub, pushed down
read-only. Budget: 1,200 words.

Not a description of the code — a reader can get that from the code. This is
the set of constraints that would be expensive to rediscover.

## Shape

The pieces and how they talk. A small diagram beats a long paragraph.

## Constraints that hold

- **…** — and why. A constraint without its reason gets removed by the next
  person who finds it inconvenient.

## What is deliberately absent

What was considered and left out, and what it cost to leave out.

## Where the shape is likely to break

The pressure points, and who decides when one gives. Naming this is what turns
an architecture record into something a builder can act on.
